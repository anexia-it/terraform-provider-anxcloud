provider_installation {
  filesystem_mirror {
    path    = "__PLUGIN_DIR__"
    include = ["hashicorp.com/anexia-it/anxcloud"]
  }

  direct {
    exclude = ["hashicorp.com/anexia-it/anxcloud"]
  }
}
